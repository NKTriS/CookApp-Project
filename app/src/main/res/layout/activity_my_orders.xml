<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingTop="36dp"
        android:paddingEnd="20dp"
        android:paddingBottom="14dp"
        android:background="#FFFFFF"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iv_back"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:padding="4dp"
            android:src="@drawable/ic_back_arrow"
            app:tint="#1A1A1A" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text="Đơn hàng của tôi"
            android:textColor="#000000"
            android:textSize="20sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Tabs -->
    <HorizontalScrollView
        android:id="@+id/hsv_tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:background="#FFFFFF"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp"
        app:layout_constraintTop_toBottomOf="@id/header">

        <LinearLayout
            android:id="@+id/ll_tabs"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView android:id="@+id/tab_all"
                android:layout_width="wrap_content" android:layout_height="36dp"
                android:gravity="center" android:paddingStart="16dp" android:paddingEnd="16dp"
                android:text="Tất cả" android:textColor="#FFFFFF" android:textStyle="bold"
                android:background="@drawable/bg_pill_ff7043" android:backgroundTint="#FF383C"
                android:layout_marginEnd="8dp"/>

            <TextView android:id="@+id/tab_pending"
                android:layout_width="wrap_content" android:layout_height="36dp"
                android:gravity="center" android:paddingStart="16dp" android:paddingEnd="16dp"
                android:text="Chờ xác nhận" android:textColor="#1A1A1A" android:textStyle="normal"
                android:background="@drawable/bg_pill_ff7043" android:backgroundTint="#F0F0F0"
                android:layout_marginEnd="8dp"/>

            <TextView android:id="@+id/tab_delivering"
                android:layout_width="wrap_content" android:layout_height="36dp"
                android:gravity="center" android:paddingStart="16dp" android:paddingEnd="16dp"
                android:text="Đang giao" android:textColor="#1A1A1A" android:textStyle="normal"
                android:background="@drawable/bg_pill_ff7043" android:backgroundTint="#F0F0F0"
                android:layout_marginEnd="8dp"/>

            <TextView android:id="@+id/tab_completed"
                android:layout_width="wrap_content" android:layout_height="36dp"
                android:gravity="center" android:paddingStart="16dp" android:paddingEnd="16dp"
                android:text="Hoàn thành" android:textColor="#1A1A1A" android:textStyle="normal"
                android:background="@drawable/bg_pill_ff7043" android:backgroundTint="#F0F0F0"
                android:layout_marginEnd="8dp"/>

            <TextView android:id="@+id/tab_cancelled"
                android:layout_width="wrap_content" android:layout_height="36dp"
                android:gravity="center" android:paddingStart="16dp" android:paddingEnd="16dp"
                android:text="Đã hủy" android:textColor="#1A1A1A" android:textStyle="normal"
                android:background="@drawable/bg_pill_ff7043" android:backgroundTint="#F0F0F0"/>
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Empty state -->
    <TextView
        android:id="@+id/tv_empty_orders"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bạn chưa có đơn hàng nào 🛒"
        android:textColor="#8E8E93"
        android:textSize="15sp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/hsv_tabs"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Orders list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_orders"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="16dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/hsv_tabs"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
